<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema personalizado para o SuperID -->
    <style name="Theme.SuperID" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Cores base -->
        <item name="colorPrimary">@color/gold_yellow</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorSecondary">@color/dark_gray</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/dark_gray</item> <!-- Barra de status -->

        <!-- Outras cores -->

        <item name="colorOnBackground">@color/dark_gray</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/dark_gray</item>
    </style>

</resources>

